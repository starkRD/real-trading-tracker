'use client';
import Link from 'next/link';import {usePathname} from 'next/navigation';import {BarChart3,WalletCards,Upload,Settings,PlusCircle} from 'lucide-react';
const links=[['/','Dashboard',BarChart3],['/positions','Positions',WalletCards],['/trades','Transactions',PlusCircle],['/import','Import TRADES',Upload],['/settings','Settings',Settings]] as const;
export default function Sidebar(){const p=usePathname();return <aside className="fixed inset-y-0 left-0 hidden w-64 border-r border-zinc-800 bg-[#090a0c] p-5 md:block"><div className="mb-8"><div className="text-lg font-bold">Real Trading Tracker</div><div className="mt-1 text-xs text-zinc-500">7Bar + actual trading ledger</div></div><nav className="space-y-1">{links.map(([href,label,Icon])=><Link key={href} href={href} className={`flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm ${p===href?'bg-zinc-800 text-white':'text-zinc-400 hover:bg-zinc-900 hover:text-white'}`}><Icon size={17}/>{label}</Link>)}</nav><div className="absolute bottom-5 left-5 right-5 rounded-xl border border-zinc-800 p-3 text-xs text-zinc-500">V2: manual ledger + TRADES import. Broker integration is V4.</div></aside>}
