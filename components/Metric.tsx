export default function Metric({label,pctText,value,sub,positive}:{label:string;pctText?:string;value?:string;sub?:string;positive?:boolean}){return <div className="card p-5"><div className="label">{label}</div>{pctText&&<div className={`mt-2 text-3xl font-bold ${positive===true?'text-emerald-400':positive===false?'text-red-400':''}`}>{pctText}</div>}{value&&<div className="mt-1 text-lg font-semibold">{value}</div>}{sub&&<div className="mt-1 text-xs text-zinc-500">{sub}</div>}</div>}
