"use client"; import {useEffect,useState} from 'react'; import {capital,saveCapital,reset} from '@/lib/store'; export default function Settings(){const[c,setC]=useState('300000');useEffect(()=>setC(String(capital())),[]);return <><h1 className="text-3xl font-bold">Settings</h1><div className="card mt-6 max-w-lg p-6"><label className="text-xs text-zinc-500">STARTING CAPITAL</label><input className="mt-2 w-full rounded-xl border border-zinc-700 bg-zinc-900 p-3" value={c} onChange={e=>setC(e.target.value)}/><button className="primary mt-4" onClick={()=>{saveCapital(Number(c));alert('Saved')}}>Save</button></div><div className="card mt-6 max-w-lg border-red-900 p-6"><b className="text-red-300">Reset local data</b><button className="mt-4 block rounded-xl border border-red-900 px-4 py-2 text-sm text-red-300" onClick={()=>{reset();location.reload()}}>Reset everything</button></div></>}
