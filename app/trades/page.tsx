"use client"; import {useEffect,useState} from 'react'; import {actuals} from '@/lib/store'; import {gross,costs,net} from '@/lib/calc';
const money=(n:number)=>new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:0}).format(n);
export default function Trades(){const[t,setT]=useState<any[]>([]);useEffect(()=>setT(actuals()),[]);return <><h1 className="text-3xl font-bold">Actual Trades</h1><p className="mt-2 text-sm text-zinc-400">Partial exits remain one trade record.</p><div className="card mt-6 overflow-auto"><table className="w-full min-w-[850px] text-sm"><thead className="border-b border-zinc-800 text-left text-xs text-zinc-500"><tr><th className="p-4">Ticker</th><th>Qty</th><th>Buy</th><th>Sell 1</th><th>Sell 2</th><th>Gross</th><th>Costs</th><th>Net</th><th>Reason</th></tr></thead><tbody>{t.map(x=><tr className="border-b border-zinc-900" key={x.id}><td className="p-4 font-semibold">{x.ticker}</td><td>{x.qty}</td><td>₹{x.buy}</td><td>{x.sell1||'—'}</td><td>{x.sell2||'—'}</td><td>{money(gross(x))}</td><td>{money(costs(x))}</td><td>{money(net(x))}</td><td className="text-zinc-400">{x.reason||'—'}</td></tr>)}</tbody></table>{!t.length&&<div className="p-10 text-center text-zinc-500">Import your TRADES file.</div>}</div></>}
