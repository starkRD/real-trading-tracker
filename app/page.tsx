"use client"; import {useEffect,useState} from 'react'; import Metric from '@/components/Metric'; import {actuals,models,capital} from '@/lib/store'; import {gross,costs,net,modelPnl} from '@/lib/calc';
const money=(n:number)=>new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:0}).format(n);
export default function Home(){const[a,setA]=useState<any[]>([]),[m,setM]=useState<any[]>([]),[c,setC]=useState(300000);useEffect(()=>{setA(actuals());setM(models());setC(capital())},[]);const g=a.reduce((s,t)=>s+gross(t),0),co=a.reduce((s,t)=>s+costs(t),0),n=a.reduce((s,t)=>s+net(t),0),mp=m.reduce((s,t)=>s+modelPnl(t,c),0);return <><h1 className="text-3xl font-bold">Real vs 7Bar</h1><p className="mt-2 text-sm text-zinc-400">Model benchmark vs what your broker actually executed.</p><div className="grid gap-4 md:grid-cols-4 mt-7"><Metric label="Capital" value={money(c)}/><Metric label="7Bar model" value={money(mp)}/><Metric label="Actual gross" value={money(g)}/><Metric label="Actual net" value={money(n)} sub={`Costs: ${money(co)}`}/></div><div className="card mt-6 p-6"><b>Core principle</b><p className="mt-2 text-sm text-zinc-400">Never replace actual execution with the theoretical price. If a GTT at ₹3,215 fails and you manually exit at ₹3,171.56, ₹3,171.56 remains the actual result.</p></div></>}
